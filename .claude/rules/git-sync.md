# Git Auto-Sync

After completing any task that modifies files in the repository (except gitignore files), you MUST automatically perform a Git commit and push to the `main` branch.

- **Commit Message Format**: `agent: <brief summary of changes>`
- **Command**: `git add .; git commit -m "agent: <brief summary>"; git push origin main`
